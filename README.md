# Bio Swarm System 🧬

![Python](https://img.shields.io/badge/Python-3.12-3776AB?logo=python)
![FastAPI](https://img.shields.io/badge/FastAPI-Async-009688?logo=fastapi)
![Vue.js](https://img.shields.io/badge/Vue.js-3.x-4FC08D?logo=vue.js)
![PostgreSQL](https://img.shields.io/badge/PostgreSQL-17-336791?logo=postgresql)

**Bio Swarm** — это гибридная система управления, связывающая **Telegram Bot**, **Web Dashboard** и асинхронный **Backend** в единый организм.

Проект создан с упором на **производительность** и **экономию ресурсов**. Архитектура позволяет держать высокую нагрузку даже на слабом железе (Legacy Hardware Optimization).

---

## 🛠 Стек технологий (Tech Stack)

Все компоненты работают асинхронно для максимальной скорости (Non-blocking I/O).

*   **Core (Backend):** Python 3.12 + **FastAPI**. Полный AsyncIO.
*   **Database:** PostgreSQL 17 + **AsyncPG** (самый быстрый драйвер) + SQLAlchemy 2.0.
*   **Interface (Frontend):** **Vue 3** (Composition API) + **Vite** + TailwindCSS.
*   **Agents (Bot):** **Aiogram 3.x**. Интегрирован напрямую в ядро для работы с общей базой данных.
*   **Deploy:** Docker Compose ready.

## 🔥 Основные фичи

1.  **Единая экосистема:** Пользователь регистрируется на сайте, получает `user_id`, и связывает аккаунт с Telegram через бота.
2.  **JWT Authentication:** Безопасная система входа и регистрации.
3.  **Real-time Sync:** Данные между ботом и сайтом синхронизируются мгновенно через общую БД.
4.  **Optimized Structure:** Монолитная архитектура бэкенда экономит до 40% RAM (по сравнению с микросервисами).

---

## 🚀 Quick Start (Запуск)

### 1. Backend & Bot
Все зависимости в одном месте.

```bash
cd backend

# Создаем venv
python -m venv venv

# Windows:
venv\Scripts\activate
# Linux/Mac:
# source venv/bin/activate

# Ставим либы
pip install -r requirements.txt

# ! ВАЖНО: Создайте файл .env на основе .env.example !

# Применяем миграции БД
alembic upgrade head

# Запуск API (Port 8001)
uvicorn app.server:app --reload --port 8001
```

**Запуск Бота (в отдельном терминале):**
```bash
# Бот использует то же окружение, что и бэкенд
cd backend
# Не забудьте активировать venv!
python run_bot.py
```

### 2. Frontend
```bash
cd frontend
npm install
npm run dev
```

### 3. Доступы после запуска
*   **Frontend:** `http://localhost:5173`
*   **Backend Docs:** `http://localhost:8001/docs`

---

## ⚙️ Логика работы (Workflow)

1.  **Registration:** Юзер создает аккаунт через Vue-интерфейс.
2.  **Linking:** Бот в Telegram просит ввести `/connect <email>`.
3.  **Integration:** Система проверяет email в базе PostgreSQL и привязывает Telegram ID к профилю.
4.  **Action:** Теперь бот может управлять данными профиля, а изменения видны на сайте.

---

### Author
*Architected by **devSvyatoslav** [INTJ] | Bio Swarm Core*
```